import type { DataTranslations } from "@/i18n/data";

/** Overrides de datos en "en" (carta, platos estrella, alérgenos, etiquetas). Claves ausentes → español. */
const en: DataTranslations = {};
export default en;
