import type { DataTranslations } from "@/i18n/data";

/** Overrides de datos en "gl" (carta, platos estrella, alérgenos, etiquetas). Claves ausentes → español. */
const gl: DataTranslations = {};
export default gl;
