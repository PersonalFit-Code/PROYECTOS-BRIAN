import type { DataTranslations } from "@/i18n/data";

/** Overrides de datos en "pt" (carta, platos estrella, alérgenos, etiquetas). Claves ausentes → español. */
const pt: DataTranslations = {};
export default pt;
